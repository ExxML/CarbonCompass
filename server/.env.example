# Server-side key, never sent to the browser. Restrict it to the Directions API
# only; referrer restrictions do not apply, as server requests send no referrer.
GOOGLE_MAPS_API_KEY=

# Set to "development" to return error details in API responses.
NODE_ENV=development
